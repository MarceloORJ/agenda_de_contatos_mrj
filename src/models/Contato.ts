class Contato {
  id: number
  name: string
  email: string
  tell: number

  constructor(id: number, name: string, email: string, tell: number) {
    this.id = id
    this.name = name
    this.email = email
    this.tell = tell
  }
}

export default Contato
